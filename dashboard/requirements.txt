flask>=3.0.0
# Pin flask-socketio and python-socketio to the 5.x series.
# Flask-SocketIO 5.x requires python-socketio 5.x, which uses the Socket.IO
# protocol v5 / Engine.IO v4 (EIO4).  The matching JavaScript client is
# socket.io 4.x (see templates/index.html and templates/story_live.html).
# Upgrading to 6.x without updating the JS CDN link will cause EIO version
# mismatch errors in the browser.  See dashboard/README.md – Troubleshooting.
flask-socketio>=5.3.0,<6
python-socketio>=5.3.0,<6
# eventlet is the production WSGI server used by Flask-SocketIO (async_mode="eventlet").
# It replaces Werkzeug's development server, which does not work reliably under
# systemd/production environments.  simple-websocket is kept as a fallback for
# environments where eventlet cannot be installed (e.g. some Windows setups).
eventlet>=0.33.0
simple-websocket>=0.10.0
