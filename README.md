# README

## Quick Start

1. Step 1 instructions...
2. Recommended method: Run `./setup.sh` (using `chmod +x setup.sh; ./setup.sh`)
   
   Alternative method: Manually set up by executing `cp .env.example .env` and then manually editing the .env file.

... Other content remains unchanged ...